import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#1976d2', // blue
    },
    secondary: {
      main: '#9c27b0', // purple
    },
    error: {
      main: '#f44336', // red
    },
    background: {
      default: '#f4f6f8',
    },
  },
});
